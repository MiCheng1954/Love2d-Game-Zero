-- tests/.busted
-- busted 配置文件
return {
    default = {
        -- 从项目根目录运行，保证 require 路径正确
        lua         = "lua",
        pattern     = "_spec%.lua$|test_.*%.lua$",
        helper      = "tests/helper.lua",
        -- 递归扫描 tests/ 目录
        ["recursive"] = true,
    }
}
